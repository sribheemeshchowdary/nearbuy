# Nearbuy

Singapore business directory built with Vite, React, TypeScript, shadcn-ui, and Tailwind CSS.

## Architecture and database

See [ARCHITECTURE.md](./ARCHITECTURE.md) for the complete website structure, Firestore collections and fields, role permissions, application workflows, external services, and Hostinger deployment model.

## Getting started

```sh
npm install
npm run dev
```

The local development server runs at `http://localhost:5173/`.

## Available scripts

- `npm run dev` starts the Vite development server.
- `npm run build` creates a production build.
- `npm run build:dev` builds using the development mode config.
- `npm run lint` runs ESLint.
- `npm run test` runs the Vitest suite once.
- `npm run test:watch` runs Vitest in watch mode.
